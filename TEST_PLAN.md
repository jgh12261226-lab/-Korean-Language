# 국립국어원 메인 페이지 테스트 계획서

본 테스트 계획서는 국립국어원 웹사이트 메인 페이지(`index.html`)의 핵심 기능 및 웹 접근성을 검증하기 위한 시나리오를 정의합니다. 모든 테스트 시나리오는 BDD(Behavior-Driven Development)의 핵심인 **Given-When-Then(주어진 상황 - 행동 - 결과)** 형식을 엄격히 준수하여 작성되었습니다.

---

## 1. 웹 접근성 (Web Accessibility - KWCAG 2.2)

### 1.1. 자동화된 접근성 기준 준수
- **Given**: 웹 접근성 검사 도구(Playwright + Axe-core)가 세팅된 테스트 환경이 주어졌을 때
- **When**: 테스트 스크립트가 메인 페이지(`index.html`)를 렌더링하고 분석을 실행하면
- **Then**: 페이지 내의 모든 요소가 명도 대비, ARIA 속성, 이미지 대체 텍스트(alt) 등의 접근성 위반 사항(Violations) 없이 검사를 통과해야 한다.

---

## 2. 히어로 슬라이더 (Hero Slider)

### 2.1. 수동 네비게이션 제어
- **Given**: 메인 페이지가 완전히 로드되어 히어로 슬라이더가 표시된 상태에서
- **When**: 사용자가 슬라이더의 '다음(Next)' 또는 '이전(Prev)' 제어 버튼을 클릭하면
- **Then**: 슬라이더 이미지가 해당 방향으로 부드럽게 전환되고, 화면에 포커스 된 슬라이드 콘텐츠가 올바르게 업데이트되어야 한다.

### 2.2. 자동 재생 및 일시정지 제어
- **Given**: 히어로 슬라이더가 일정 시간(4초) 간격으로 자동 재생 중인 상황에서
- **When**: 사용자가 '일시정지(Pause)' 버튼을 클릭하면
- **Then**: 슬라이더의 자동 재생이 즉시 중단되고, 버튼의 상태가 '재생(Play)'으로 변경되어 시각적으로 표시되어야 한다.

---

## 3. 다국어 지원 (Multilingual Toggle)

### 3.1. 클라이언트 사이드 언어 전환
- **Given**: 기본 언어(한국어)로 설정된 메인 페이지를 보고 있는 상태에서
- **When**: 사용자가 GNB(글로벌 내비게이션 바)에 위치한 언어 전환 토글(KO/EN)을 클릭하면
- **Then**: 페이지 새로고침(Reload) 없이 메인 메뉴, 검색창 플레이스홀더 등 UI 내의 지정된 텍스트 요소들이 즉시 영어로 변경되어야 한다.

---

## 4. 모바일 슬라이드 아웃 메뉴 (Off-canvas Menu)

### 4.1. 메뉴 토글 및 배경 스크롤 제어
- **Given**: 모바일 화면 크기(예: 768px 이하)에서 메인 페이지를 탐색 중일 때
- **When**: 사용자가 상단 헤더의 햄버거(메뉴) 버튼을 클릭하면
- **Then**: 슬라이드 아웃 메뉴 패널이 화면 측면에서 나타나며, 동시에 바탕이 되는 메인 페이지의 상하 스크롤 기능이 잠금(Scroll Lock) 처리되어야 한다.

### 4.2. 키보드 접근성 및 포커스 트랩 (Focus Trap)
- **Given**: 화면 접근성 기기(키보드 등)를 사용하여 모바일 슬라이드 아웃 메뉴를 열어둔 상태에서
- **When**: 사용자가 `Tab` 키를 반복해서 눌러 메뉴 요소들을 탐색하면
- **Then**: 키보드 초점(Focus)이 슬라이드 메뉴 외부 배경으로 빠져나가지 않고, 닫기 버튼 및 메뉴 링크들 내부에서만 순환되어야 한다.

---

## 5. 검색창 UI (Search UI Overlay)

### 5.1. 검색창 포커스 시각 피드백
- **Given**: 히어로 이미지 위에 오버레이된 검색창이 화면에 표시된 상태에서
- **When**: 사용자가 검색어 입력 필드를 클릭하거나 `Tab` 키를 통해 포커스(Focus)를 맞추면
- **Then**: 접근성 가이드라인에 맞게 검색창 입력 영역 주변에 명확하고 두꺼운 아웃라인(또는 그림자)이 나타나 현재 활성화된 상태임을 시각적으로 인지할 수 있어야 한다.
